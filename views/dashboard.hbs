{{> menu id="dashboard"}}

<section class="ui segment">
  {{#each playlists}}
      <h2 class="ui header">
        {{title}}
      </h2>
      {{> listsongs}}
  {{/each}}

 <p>
  <span id="rating"> </span>
</p>

<button class="ui yellow button">
  Rate it! <i class="star icon"></i>
</button>
</section>